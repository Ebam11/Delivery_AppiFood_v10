DIAGRAMA ESTRUCTURA PROYECTO VSCode AppiFood v10

BACKEND (Laravel API)
├── app/Models/
│   ├── User.php (role: admin/restaurant/user)
│   ├── Restaurant.php (user_id, products, orders)
│   ├── Order.php (status transitions)
│   └── Product.php (CRUD restaurant)
├── app/Http/Controllers/API/Restaurant/
│   ├── DashboardController.php (stats revenue/orders)
│   ├── ProfileRestaurantController.php (logo upload)
│   ├── ProductController.php (CRUD + toggle)
│   └── OrderManagementController.php (status update)
├── routes/api.php (middleware role:restaurant)
└── package.json ("dev": "php artisan serve --port=5173")

FRONTEND (React 18 + Vite)
├── src/App.jsx
│   ├── PublicLayout (Home/Restaurants)
│   ├── RoleRoute (role guard)
│   └── Routes /restaurant/* → RestaurantLayout
├── src/pages/Restaurant/
│   ├── RestaurantLayout.jsx (sidebar + Header reutilizado)
│   └── RestaurantDashboard.jsx (fetch /api/restaurant/dashboard)
├── src/components/
│   └── ErrorMessage.jsx (export default FIXED)
├── src/store/
│   ├── authStore.js
│   └── restaurantStore.js (stats)
├── vite.config.js (proxy /api → Backend:5173)
└── package.json (React Router, Zustand, Tailwind)

CAMBIOS HECHOS:
1. ErrorMessage.jsx → export default
2. RestaurantLayout.jsx → Sidebar responsive
3. RestaurantDashboard.jsx → Stats cards API
4. App.jsx → /restaurant routes + RoleGuard
5. Backend/package.json → npm run dev=API port 5173

COMANDOS:
Backend> npm run dev → localhost:5173 (API)
Frontend> npm run dev → localhost:5173 (React SPA)

PROBLEMA: Frontend blank/white screen localhost:5173
F12→Console/Network revisar errors 404/imports

